# Copy this content into the Streamlit Community Cloud → App settings → Secrets editor.
# Do NOT commit a secrets.toml with real values — it is gitignored.

SNOWFLAKE_ACCOUNT = "vrsugdf-gx74482"
SNOWFLAKE_USER = "dbt"
SNOWFLAKE_ROLE = "TRANSFORM"
SNOWFLAKE_WAREHOUSE = "COMPUTE_WH"
SNOWFLAKE_DATABASE = "OLIST"
SNOWFLAKE_SCHEMA = "DEV"

# Inline PEM key (replace newlines with \n literal as shown)
SNOWFLAKE_PRIVATE_KEY_BODY = "-----BEGIN ENCRYPTED PRIVATE KEY-----\n...\n-----END ENCRYPTED PRIVATE KEY-----\n"
SNOWFLAKE_PRIVATE_KEY_PASSPHRASE = "your_passphrase"
